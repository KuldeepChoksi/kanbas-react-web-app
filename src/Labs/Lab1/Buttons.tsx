export default function Buttons(){
    return (
        <div>
            <h5 id="wd-buttons">Buttons</h5>
    <button id="wd-all-good" onClick={() => alert("Life is Good!, but it can be better")} type="button">
        Hello World!
    </button>
        </div>
    )
}